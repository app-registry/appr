codecov:
  notify:
    require_ci_to_pass: yes
  token: 82510329-4647-4e4d-88ee-98ee1b2536c4

coverage:
  precision: 1
  round: down
  range: "70...100"

  status:
    project: yes
    patch: yes
    changes: no

parsers:
  gcov:
    branch_detection:
      conditional: yes
      loop: yes
      method: no
      macro: no

comment: off
  # layout: "reach, diff, flags, files, footer"
  # behavior: default
  # require_changes: no
