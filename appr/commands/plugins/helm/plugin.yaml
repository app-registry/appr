name: "registry"
version: "0.5.0"
usage: "Integreate APPR client to HELM"
description: |-
  This plugin provides APPR client to Helm.
ignoreFlags: false
useTunnel: false
command: "$HELM_PLUGIN_DIR/appr.sh"
